VLLM_VERSION=latest

VLLM_HOST_PORT=8000

# Model is loaded at startup, unlike Ollama's pull-on-demand.
VLLM_MODEL=Qwen/Qwen2.5-1.5B-Instruct

# Required for gated models (Llama, Gemma, ...). Leave empty for open ones.
VLLM_HUGGING_FACE_HUB_TOKEN=

VLLM_SHM_SIZE=8gb
