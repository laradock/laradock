services:

    mailpit:
      restart: always
      build: ./mailpit
      ports:
        - "${MAILPIT_HTTP_PORT}:8025"
        - "${MAILPIT_SMTP_PORT}:1025"
      networks:
        - frontend
        - backend
