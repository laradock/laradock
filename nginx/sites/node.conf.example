# =====================================
# HTTP configuration
# =====================================
server {
    listen 80;
    listen [::]:80;
    server_name node.com www.node.com;

    location / {
        proxy_pass http://node.com:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_cache_bypass $http_upgrade;
    }

    location ~ /\.ht {
        deny all;
    }

    location /.well-known/acme-challenge/ {
        root /var/www/letsencrypt/;
        log_not_found off;
    }

    error_log /var/log/nginx/node_error.log;
    access_log /var/log/nginx/node_access.log;
}
# =====================================
# End HTTP configuration
# =====================================


# =====================================
# # To enable SSL for this site, use the following configuration instead of the above.
# =====================================


# =====================================
# HTTPS configuration
# =====================================
# server {
#     listen 80;
#     listen [::]:80;
#     server_name node.com www.node.test;

#     location /.well-known/acme-challenge/ {
#         root /var/www/letsencrypt;
#     }

#     location / {
#         return 301 https://node.com$request_uri;
#     }
# }

# server {
#     listen 443 ssl;
#     server_name www.node.test;

#     # Self certificates #
#     ssl_certificate /var/certs/node.com/cert.pem;
#     ssl_certificate_key /var/certs/node.com/privkey.pem;
#     # Certbot certificates #
#     # ssl_certificate /etc/letsencrypt/live/node.com/fullchain.pem;
#     # ssl_certificate_key /etc/letsencrypt/live/node.com/privkey.pem;

#     return 301 https://node.com$request_uri;
# }

# server {
#     listen 443;
#     listen [::]:443;
#     server_name node.com;

#     # Self certificates #
#     ssl_certificate /var/certs/node.com/cert.pem;
#     ssl_certificate_key /var/certs/node.com/privkey.pem;
#     # Certbot certificates #
#     # ssl_certificate /etc/letsencrypt/live/node.com/fullchain.pem;
#     # ssl_certificate_key /etc/letsencrypt/live/node.com/privkey.pem;

#     ssl_protocols       SSLv3 TLSv1 TLSv1.1 TLSv1.2;
#     ssl_ciphers         HIGH:!aNULL:!MD5;

#     location / {
#         proxy_pass http://node.com:3001;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_cache_bypass $http_upgrade;
#     }

#     location ~ /\.ht {
#         deny all;
#     }

#     location /.well-known/acme-challenge/ {
#         root /var/www/letsencrypt/;
#         log_not_found off;
#     }

#     error_log /var/log/nginx/node_error.log;
#     access_log /var/log/nginx/node_access.log;
# }
# =====================================
# End HTTPS configuration
# =====================================
