services:

    ollama:
      image: ollama/ollama:${OLLAMA_VERSION}
      volumes:
        - ollama:/root/.ollama
      ports:
        - "${OLLAMA_HOST_PORT}:11434"
      networks:
        - frontend
        - backend
